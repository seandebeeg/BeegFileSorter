<Window x:Class="FileSorter.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FileSorter Settings" Height="150" Width="520"
        Loaded="Window_Loaded">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 8">
            <TextBlock Text="Watch Path:" Width="110" VerticalAlignment="Center"/>
            <TextBox x:Name="WatchPathBox" Width="320"/>
            <Button Content="..." Width="28" Margin="6,0,0,0" Click="BrowseWatch"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 8">
            <TextBlock Text="Destination:" Width="110" VerticalAlignment="Center"/>
            <TextBox x:Name="DestPathBox" Width="320"/>
            <Button Content="..." Width="28" Margin="6,0,0,0" Click="BrowseDest"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Save Settings" Width="160" Height="28" HorizontalAlignment="Left" Click="SaveSettings"/>

    <TextBlock x:Name="StatusText" Grid.Row="2" Margin="180,0,0,0" VerticalAlignment="Center"/>
  </Grid>
</Window>
